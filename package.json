{
  "name": "express-downloader-api",
  "version": "1.0.0",
  "description": "An Express API for downloading videos and audio using yt-dlp",
  "main": "./app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "yt-dlp",
    "video-downloader",
    "audio-downloader",
    "api"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5", // For enabling CORS (optional)
    "dotenv": "^16.0.3", // For environment variables
    "express": "^4.18.2", // Express framework
    "node-fetch": "^2.6.7", // For fetching files (used in yt-dlp download)
    "yt-dlp-exec": "^1.2.0" // A wrapper for yt-dlp (optional, but recommended)
  },
  "devDependencies": {
    "nodemon": "^2.0.20" // For development (auto-restart server)
  },
  "engines": {
    "node": "16.x", // Specify the Node.js version
    "npm": "8.x" // Specify the npm version
  }
}
